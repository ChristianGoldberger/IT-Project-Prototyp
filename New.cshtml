@model PagedList.IPagedList<Helper.QuestionAnswer>
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

@{
    ViewBag.Title = "Enter new decision";
}

<h2>@ViewBag.myQ.QuestionKey</h2>
<br />
<!-- Radio buttons platzieren etc.. -->
  


    Gewichtung wählen (1=wenig, 6=viel)<br/>


  <form name="radioButtonForm">                 <fieldset name="test">             <input type="radio" id="decision1" name="Entscheidung" value="1">             <label for="Radio1"> 1</label>             <input type="radio" id="decision2" name="Entscheidung" value="2">             <label for="Radio2"> 2</label>             <input type="radio" id="decision3" name="Entscheidung" value="3">             <label for="Radio3">3</label>              <input type="radio" id="decision4" name="Entscheidung" value="4">             <label for="Radio4">4</label>                 <input type="radio" id="decision5" name="Entscheidung" value="5">             <label for="Radio5">5</label>                 <input type="radio" id="decision6" name="Entscheidung" value="6">             <label for="Radio">6</label>                      </fieldset>     </form> 
 
   

<div class="col-sm-5 col-lg-5">
     
            <a id="decisionClick" class="btn btn-primary" style="margin-bottom:5px" href="@Url.Action("New", new {q = Model.PageNumber+1})">Speichern</a>
          


              
        
   <!--
   a class="btn btn-primary" style="margin-bottom:5px" href='javascript:history.go(-1)'>Zurück</a>
    <input type="submit" name="decisionSubmit" value="Speichern" class="btn btn-primary" /> 
    href="@Url.Action("New", new { controller = "Decision" })
    -->
            
</div>
</div>
   
<br />

Question @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, q => Url.Action("New", new { q }))

@section scripts
{
    <script src="~/Scripts/newDecision.js" type="text/javascript"></script>
    <script src="~/Scripts/decision.js" type="text/javascript"></script>
 
 
}